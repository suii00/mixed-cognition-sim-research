# Derived artifacts

Versioned metrics, reports, images, and videos belong here. Raw run directories
under `runs/` are immutable; derivations must write to a new timestamped
directory and record their source run and metric/tool version.
